<form method = "post" action = "/confirm">
<%meat.each do |mv| %>
<p><%=mv%>
	<input type = "radio" value = "<%=mv%>" name = "confirmed[<%=mv%>]" checked> Yes
 <input type = "radio" value = "no" name = "confirmed[<%=mv%>]"> No </p>
<%end%>

<%cheese.each do |cv| %>
<p><%=cv%>
	<input type = "radio" value = "<%=cv%>" name = "confirmed[<%=cv%>]" checked> Yes
 <input type = "radio" value = "no" name = "confirmed[<%=cv%>]"> No</p> 
<%end%>

<%veg.each do |vv| %>
<p><%=vv%>  
	<input type = "radio" value = "<%=vv%>" name = "confirmed[<%=vv%>]" checked> Yes
 <input type = "radio" value = "no" name = "confirmed[<%=vv%>]"> No </p>
<%end%>

<%sauce.each do |sv|%>   
   <p><%=sv%>
	<input type = "radio" value = "<%=sv%>" name = "confirmed[<%=sv%>]" checked> Yes
  <input type = "radio" value = "no" name = "confirmed[<%=sv%>]"> No </p>
<%end%>

<input type="submit" value="confirm">
</form>